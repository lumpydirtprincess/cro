// The MIT License (MIT)
// © mihakralj
//@version=6

// 
// Category: volatility
// Auto-converted to TradingView v6 Library
//
library("", "v1.0.0", overlay=false)


//@function Calculates the True Range
//@returns The True Range value for the current bar.
export tr() =>
    float prevClose = nz(close[1], close)
    float tr1 = high - low
    float tr2 = math.abs(high - prevClose)
    float tr3 = math.abs(low - prevClose)
    float trueRange = math.max(tr1, math.max(tr2, tr3))
    trueRange


// ---------- Main loop ----------

// Calculation
trValue = tr()

// Plot
// plot(trValue, "TR", color=color.yellow, linewidth=2)  // Disabled in library

