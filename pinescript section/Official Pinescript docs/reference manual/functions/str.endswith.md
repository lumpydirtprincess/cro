# str.endswith()

3 overloads

Returns true if the `source` string ends with the substring specified in `str`, false otherwise.

Syntax & Overloads

[```\\
str.endswith(source, str) → const bool\\
```](https://www.tradingview.com/pine-script-reference/v6/#fun_str.endswith-0) [```\\
str.endswith(source, str) → simple bool\\
```](https://www.tradingview.com/pine-script-reference/v6/#fun_str.endswith-1) [```\\
str.endswith(source, str) → series bool\\
```](https://www.tradingview.com/pine-script-reference/v6/#fun_str.endswith-2)

Arguments

source (const string) Source string.

str (const string) The substring to search for.

Returns

True if the `source` string ends with the substring specified in `str`, false otherwise.

See also

[str.startswith()](https://www.tradingview.com/pine-script-reference/v6/#fun_str.startswith)
